<div class="row align-items-center mb-4">
  <div class="col">
    <h1 class="fw-bold">Δίκτυο Χρηστών</h1>
    <p class="text-muted">Αναζήτησε συνεργάτες για τις εργαστηριακές σου ασκήσεις.</p>
  </div>
</div>

<div class="row">
  <% @users.each do |user| %>
    <div class="col-md-4 mb-4">
      <div class="card border-0 shadow-sm h-100 rounded-4">
        <div class="card-body text-center p-4">
          <div class="bg-primary text-white rounded-circle d-inline-flex align-items-center justify-content-center mb-3" style="width: 60px; height: 60px; font-size: 24px;">
            <%= user.name.to_s[0].upcase if user.name.present? %>
          </div>
          <h5 class="card-title fw-bold"><%= user.name.presence || "Ανώνυμος Χρήστης" %></h5>
          <p class="card-text text-muted small"><%= user.email %></p>
          
          <div class="mt-3">
            <% if current_user.friends.include?(user) %>
              <button class="btn btn-light w-100 rounded-pill disabled">✅ Στις επαφές σας</button>
            <% else %>
              <%= button_to "Προσθήκη Επαφής", add_friend_user_path(user), method: :post, class: "btn btn-outline-primary w-100 rounded-pill" %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>

<hr class="my-5">

<h3 class="fw-bold mb-4">Οι Επαφές μου (<%= current_user.friends.count %>)</h3>
<div class="row">
  <% current_user.friends.each do |friend| %>
    <div class="col-md-4 mb-3">
      <div class="p-3 bg-white shadow-sm rounded-4 border-start border-primary border-4 d-flex justify-content-between align-items-center">
        <div>
          <div class="fw-bold"><%= friend.name.presence || friend.email %></div>
          <small class="text-success">● Online</small>
        </div>
        <div>
          <%= button_to remove_friend_user_path(friend), method: :delete, data: { confirm: "Είστε σίγουροι;" }, class: "btn btn-link text-danger p-0", title: "Διαγραφή επαφής" do %>
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" class="bi bi-person-x-fill" viewBox="0 0 16 16">
              <path fill-rule="evenodd" d="M1 14s-1 0-1-1 1-4 6-4 6 3 6 4-1 1-1 1zm5-6a3 3 0 1 0 0-6 3 3 0 0 0 0 6m6.146-2.854a.5.5 0 0 1 .708 0L14 6.293l1.146-1.147a.5.5 0 0 1 .708.708L14.707 7l1.147 1.146a.5.5 0 0 1-.708.708L14 7.707l-1.146 1.147a.5.5 0 0 1-.708-.708L13.293 7l-1.147-1.146a.5.5 0 0 1 0-.708"/>
            </svg>
          <% end %>
        </div>
      </div>
    </div>
  <% end %>
</div>